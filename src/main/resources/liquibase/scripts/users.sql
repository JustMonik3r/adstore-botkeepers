 create table users(
 id integer generated by default as identity primary key,
 email text,
 first_name varchar,
 last_name varchar,
 phone text,
 role text,
 password varchar,
 image_id integer references image(id)
 );